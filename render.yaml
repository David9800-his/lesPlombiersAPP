services:
  - type: web
    name: agent-stats-hockey
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python agent_stats_hockey.py
    envVars:
      - key: PORT
        value: 10000
